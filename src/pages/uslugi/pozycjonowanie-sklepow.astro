---
import BaseLayout from '../../layouts/BaseLayout.astro'
import InlineEditor from '../../components/InlineEditor.astro'
import content from '../../data/pages/uslugi-pozycjonowanie-sklepow.json'

const { results, items, platforms } = content
---

<BaseLayout
  title="Pozycjonowanie Sklepów Internetowych — SEO dla E-commerce | KrPage"
  description="Pozycjonowanie sklepów WooCommerce, PrestaShop, Shopify. Optymalizacja kategorii, kart produktów i struktury technicznej. Więcej ruchu organicznego = więcej sprzedaży."
  schemaType="service"
  schemaProps={{ serviceName: 'Pozycjonowanie Sklepów Internetowych' }}
>

  <!-- HERO -->
  <section class="max-w-7xl mx-auto px-6 md:px-12 pt-16 pb-xl relative overflow-hidden">
    <div class="absolute -top-32 -left-32 w-[500px] h-[500px] bg-neon/5 blur-[120px] rounded-full pointer-events-none"></div>
    <div class="grid lg:grid-cols-2 gap-lg items-center">
      <div class="space-y-6 z-10">
        <div class="section-label" data-cms="heroEyebrow">{content.heroEyebrow}</div>
        <h1 class="text-headline-xl text-on-surface">
          <span data-cms="heroTitleL1">{content.heroTitleL1}</span><br />
          <span class="text-neon" data-cms="heroTitleAccent">{content.heroTitleAccent}</span>
        </h1>
        <p class="text-body-lg text-on-surface-variant max-w-xl" data-cms="heroDesc">
          {content.heroDesc}
        </p>
        <div class="flex flex-wrap gap-md">
          <a href="/kontakt"
             class="inline-flex items-center gap-2 bg-neon text-on-primary font-bold text-label-md uppercase tracking-widest px-lg py-md hover:brightness-110 active:scale-95 transition-all shadow-neon-sm">
            <span aria-hidden="true" class="material-symbols-outlined text-sm">calendar_month</span>
            <span data-cms="heroBtnPrimary">{content.heroBtnPrimary}</span>
          </a>
          <a href="/uslugi/audyt-seo"
             class="inline-flex items-center gap-2 border border-neon/30 text-neon font-bold text-label-md uppercase tracking-widest px-lg py-md hover:bg-neon/5 transition-all">
            <span data-cms="heroBtnSecondary">{content.heroBtnSecondary}</span>
          </a>
        </div>
      </div>

      <div class="bento-card p-lg">
        <div class="text-label-sm text-neon uppercase tracking-widest mb-md" data-cms="resultsLabel">{content.resultsLabel}</div>
        <div class="grid grid-cols-2 gap-md">
          {results.map((s, i) => (
            <div class="bg-surface-container p-md border border-outline-variant/30">
              <p class="text-headline-md text-neon font-black mb-xs" data-cms={`results.${i}.value`}>{s.value}</p>
              <p class="text-label-sm text-on-surface uppercase tracking-wide mb-1" data-cms={`results.${i}.label`}>{s.label}</p>
              <p class="text-[10px] text-on-surface-variant/60 font-mono" data-cms={`results.${i}.sub`}>{s.sub}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- ZAKRES -->
  <section class="bg-surface-container-low border-y border-white/5 py-xl">
    <div class="max-w-7xl mx-auto px-6 md:px-12">
      <div class="section-label mb-sm" data-cms="zakresEyebrow">{content.zakresEyebrow}</div>
      <h2 class="text-headline-lg text-on-surface mb-lg"><span data-cms="zakresTitleL1">{content.zakresTitleL1}</span><br /><span class="text-neon" data-cms="zakresTitleAccent">{content.zakresTitleAccent}</span></h2>
      <p class="text-body-md text-on-surface-variant max-w-2xl mb-lg" data-cms="zakresDesc">
        {content.zakresDesc}
      </p>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-gutter">
        {items.map((item, i) => (
          <div class="bento-card p-lg group hover:border-neon/40 transition-all">
            <span aria-hidden="true" class="material-symbols-outlined text-neon text-3xl mb-md">{item.icon}</span>
            <h3 class="text-headline-md text-on-surface mb-sm" data-cms={`items.${i}.title`}>{item.title}</h3>
            <p class="text-body-md text-on-surface-variant" data-cms={`items.${i}.desc`}>{item.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- PLATFORMY -->
  <section class="max-w-7xl mx-auto px-6 md:px-12 py-xl">
    <div class="section-label mb-sm" data-cms="platformsEyebrow">{content.platformsEyebrow}</div>
    <h2 class="text-headline-lg text-on-surface mb-lg"><span data-cms="platformsTitleL1">{content.platformsTitleL1}</span> <span class="text-neon" data-cms="platformsTitleAccent">{content.platformsTitleAccent}</span></h2>
    <div class="grid grid-cols-2 md:grid-cols-4 gap-gutter mb-xl">
      {platforms.map((platform, i) => (
        <div class="bento-card p-lg text-center hover:border-neon/40 transition-all">
          <span aria-hidden="true" class="material-symbols-outlined text-neon text-3xl mb-sm">storefront</span>
          <p class="text-label-md text-on-surface uppercase tracking-widest" data-cms={`platforms.${i}`}>{platform}</p>
        </div>
      ))}
    </div>

    <div class="bento-card p-xl text-center relative overflow-hidden">
      <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
        <div class="w-96 h-48 bg-neon/5 blur-[80px] rounded-full"></div>
      </div>
      <div class="relative z-10">
        <h2 class="text-headline-lg text-on-surface mb-md">
          <span data-cms="ctaTitleL1">{content.ctaTitleL1}</span><br />
          <span class="text-neon" data-cms="ctaTitleAccent">{content.ctaTitleAccent}</span>
        </h2>
        <a href="/kontakt"
           class="inline-flex items-center gap-2 bg-neon text-on-primary font-bold text-label-md uppercase tracking-widest px-xl py-md hover:brightness-110 active:scale-95 transition-all shadow-neon mt-md">
          <span aria-hidden="true" class="material-symbols-outlined text-sm">troubleshoot</span>
          <span data-cms="ctaBtn">{content.ctaBtn}</span>
        </a>
      </div>
    </div>
  </section>

  <InlineEditor contentPath="src/data/pages/uslugi-pozycjonowanie-sklepow.json" />

</BaseLayout>
